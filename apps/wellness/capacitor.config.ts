import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.carebridgehome.wellness',
  appName: 'Care Bridge Wellness',
  webDir: 'dist',
};

export default config;
